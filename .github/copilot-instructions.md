# Copilot Rules

- The project uses a Conda environment named `EdapGui`.
- Always activate this environment or use `conda run -n EdapGui` when running python scripts.
- The project relies on `pywin32` for window management.
